# Andrew Lee Personal Site

## Local development

```bash
bundle install
bundle exec jekyll serve
```

## Deployment

Deployment is handled by GitHub Actions. Push `main` or `source` to GitHub and the Pages workflow will build and publish the site.
